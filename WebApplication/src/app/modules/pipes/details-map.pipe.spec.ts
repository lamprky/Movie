import { DetailsMapPipe } from './details-map.pipe';

describe('DetailsMapPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsMapPipe();
    expect(pipe).toBeTruthy();
  });
});
